{
  "name": "heimdalljs",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "build:node": "pnpm -r run build:node",
    "build:browser": "pnpm -r run build:browser",
    "build:test": "pnpm -r run build:test",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
